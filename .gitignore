/target
# Per-session git worktrees (see CLAUDE.md workflow).
/.wt
*.rs.bk
# Generated packaging assets (man page + shell completions from `keyhole gen`),
# produced by `just package` and the release/CI workflows.
/dist-assets
# Local environment overrides (e.g. docker compose host ports).
.env
# Pending insta snapshots: transient review artifacts, never committed.
*.snap.new
